{
  "name": "osint-backend",
  "version": "1.0.0",
  "description": "Backend proxy for OSINT Globe - fetches data from GDELT and other sources",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
